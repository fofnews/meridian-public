# meridian-public

## Mission

**Tell the whole story during each news cycle.**

People distrust the news because they sense they're only getting a fragment — filtered through a single outlet's framing. Meridian pieces it back together: collecting what every major source reports, surfacing where they agree, diverge, and what conclusions each reaches. The public site is how that synthesis reaches readers.

Every UI and feature decision should serve this: disagreements and unique angles deserve prominence, source attribution matters, and the display should make cross-outlet contrast legible — not flatten it into a single narrative.

## Project Overview

The public-facing Meridian news site. Displays analysis reports, raw articles, timelines, and broadcast content produced by the Meridian-Website pipeline.

**Stack:** React 19, Vite (Rolldown), Tailwind CSS v4, Express (local dev only)
**Deployment:** Vercel — static build (`dist/`) + serverless functions in `api/`
**Local dev:** `npm run dev` (Vite client on :5173 + Express server on :3002)

## Dual Server Architecture

This repo has two parallel API implementations:

| Path | Used by | Purpose |
|------|---------|---------|
| `server.js` | Local dev (`npm run dev:server`) | Express server, serves all `/api/*` routes |
| `api/*.js`, `api/*/[param].js` | Vercel (production) | Serverless functions, one file per route |

**When adding or changing an API route, update BOTH.** The serverless functions must not use Node-specific APIs unavailable in Vercel's edge/serverless runtime (no `fs` with absolute paths — use `path.join(process.cwd(), ...)` or environment variables).

## Directory Structure

```
api/                          — Vercel serverless functions (production API)
  dates.js                    — GET /api/dates
  timelines.js                — GET /api/timelines
  suggestions.js              — GET/POST /api/suggestions
  search.js                   — GET /api/search
  articles/[date].js          — GET /api/articles/:date
  report/[date].js            — GET /api/report/:date
  topics/[date].js            — GET /api/topics/:date
  suggestions/[id]/
    vote.js                   — POST /api/suggestions/:id/vote
    downvote.js               — POST /api/suggestions/:id/downvote
    done.js                   — POST /api/suggestions/:id/done
    delete.js                 — DELETE /api/suggestions/:id

src/                          — React frontend
  App.jsx                     — Main layout, date/edition state, view routing
  ThemeContext.jsx            — Dark/light theme context and toggle
  utils.js                    — Shared utilities (decodeText, etc.)
  index.css                   — Global CSS custom properties and base styles
  main.jsx                    — Vite entry point
  components/
    AnalysisView.jsx          — Claims, disagreements, and context display for a story
    ArticlesView.jsx          — Articles tab: category/subcategory filters, topic chips, search
    BroadcastStage.jsx        — Video-side map component (chyron, LIVE, ticker, scanlines); rendered by ?mode=broadcast
    DateNav.jsx               — Date and edition navigation
    ErrorBoundary.jsx         — React error boundary wrapping report/articles/timeline views
    FilmGrain.jsx             — Animated canvas noise overlay for broadcast video (gated on broadcastMode)
    MapHero.jsx               — Website-side full-width map hero (sober status strip)
    SourceBadge.jsx           — Colored source label chip
    StoryCard.jsx             — Story display with analysis sections
    SuggestionBox.jsx         — User suggestion submission and voting
    TimelineView.jsx          — Ongoing topics timeline (flattens parent/child hierarchy via buildDisplayTimelines)
  map/                        — Shared map kernel (consumed by both MapHero and BroadcastStage)
    kernel.js                 — createMap (lazy Mapbox load, projection: globe)
    layers.js                 — applyMapStyle (fog + data-driven highlight/arc/graticule layers)
    arcs.js                   — Great-circle arc GeoJSON for source-to-story lines
    sources.js                — Static HQ coordinates for news outlets (used by arcs)
    marker.js                 — Radar-pulse marker
    camera.js                 — flyToLocation, cinematicFlyTo, ambient rotation, returnToAmbient
    geocoding.js              — Headline-based location fallback (capped Map cache, 400 entries)
    graticule.js              — Pre-computed lat/lon gridline GeoJSON at 15° intervals
    terminator.js             — Day/night terminator polygon computation
    useMeridianMap.js         — Orchestration hook (refs, init, theme switch, focus + idle)

public/
  favicon.svg
  robots.txt
  sitemap.xml
  meridian-dark.style.json    — Committed Mapbox dark style; generated by scripts/build-meridian-styles.js
  meridian-light.style.json   — Committed Mapbox light style; generated by scripts/build-meridian-styles.js
  fonts/                      — Self-hosted Mapbox glyph tiles (Noto Sans, Playfair Display); generated by scripts/build-glyphs.js

scripts/
  build-meridian-styles.js    — Fetches Mapbox dark-v11/light-v11, applies Meridian customizations, writes to public/. Run: npm run build-styles
  build-glyphs.js             — One-time: downloads fonts, generates SDF PBF glyph tiles for self-hosted Mapbox fonts
  verify-styles.js            — Asserts key properties on the generated style files
  build-shotlist.js           — Generates a shotlist JSON for a broadcast edition (item 14)
  synthesize-narration.js     — ElevenLabs narration synthesis pipeline (item 17)
  record-clip.js              — Headless Playwright renderer: captures .webm from the broadcast page (item 16)
  finalize-clip.js            — ffmpeg mux: combines .webm + narration audio → MP4 (item 18)
  produce-clip.js             — End-to-end: edition ID → publishable MP4s + thumbnails (item 19)

docs/
  map-broadcast-checklist.md  — Full broadcast pipeline phases 0–2 (19 items); authoritative reference for map/broadcast work
  voice.md                    — ElevenLabs voice configuration for narration
  sessions/                   — Per-session dev notes (YYYY-MM-DD-topic.md)
  superpowers/                — Implementation plans and design specs

reports/              — Analysis JSON files (synced from Meridian-Website, read-only)
articles/             — Raw article JSON files (synced from Meridian-Website, read-only)
topics/               — Topic chip JSON files (synced from Meridian-Website, read-only)
timelines/
  timeline.json         — Ongoing topic timeline (synced from Meridian-Website, read-only)

suggestions.json      — Persisted user suggestions (local dev only, not on Vercel)
server.js             — Local Express dev server (mirrors api/ for development)
vercel.json           — SPA rewrite rules (non-/api/* → index.html) + function config
vite.config.js        — Vite build config
TODO.md               — Known open tasks
```

## Key Conventions

- All code is ES modules (`import`/`export`)
- Styling uses CSS custom properties (`var(--bg-primary)`, `var(--accent)`, etc.) — never hardcode colors
- Do not use Node-specific APIs (`fs`, `path.resolve(__dirname)`) in the `api/` serverless functions
- `vercel.json` rewrites all non-API paths to `index.html` for SPA routing — do not remove this

## Data Files

All data files are read-only on the public site — they are written by Meridian-Website and synced via git:

- `reports/YYYY-MM-DD-{morning|evening}.json` — full analysis report
- `articles/YYYY-MM-DD.json` — flat array of raw articles
- `topics/YYYY-MM-DD.json` — pre-computed topic chips (label, count, sources, keywords)
- `timelines/timeline.json` — ongoing topic timeline; flat array of topics where parents have `children` (via `parentId` on leaves) and leaves carry `entries`. The frontend flattens this hierarchy for display.

Note: the pipeline also maintains `timelines/embeddings.json` as a sidecar for candidate retrieval — it is **not** synced here and is gitignored.

## Environment Variables (Vercel)

- `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` — used by suggestions serverless functions for persistent storage on Vercel (local dev falls back to `suggestions.json`)

## Deployment

Vercel auto-deploys on push to `main`. Build command: `npm run build`. Output: `dist/`.

After making changes locally: build with `npm run build`, verify `dist/` looks correct, then push.
